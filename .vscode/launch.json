{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "python": "${command:python.interpreterPath}",
            "justMyCode": false,
            "env": {"PYTEST_ADDOPTS": "--no-cov"},
            "purpose": [
                "debug-test"
            ],
            "args": [] // è comodo esplicitare il default vuoto e andarlo a settare se necessario

        }
    ]
}
